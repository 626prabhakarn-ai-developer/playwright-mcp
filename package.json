{
  "name": "playwright-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Simple Playwright MCP test for Sauce Demo login",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.1"
  }
}
